# Safe Language Annotated SPARK Companion -- CI Workflow
# Source commit: 468cf72332724b04b7c193b4d2a3b02f1584125d
#
# This workflow builds the SPARK companion, runs GNATprove flow and proof
# analysis, extracts assumptions, and diffs them against the tracked baseline.

name: SPARK Companion CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  FROZEN_SHA: "468cf72332724b04b7c193b4d2a3b02f1584125d"

defaults:
  run:
    working-directory: companion/gen

jobs:
  execution-guard:
    name: Execution Guard
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      # Python is installed only to run the PR06.7 gate script and the unchanged
      # PR05/PR06 harnesses it invokes. `safec check` itself must not depend on
      # Python at runtime.
      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Install GNATprove
        working-directory: .
        run: |
          alr install gnatprove=14.1.1
          echo "${HOME}/.alire/bin" >> "$GITHUB_PATH"

      - name: Validate execution tracker and repo facts
        working-directory: .
        run: python3 scripts/validate_execution_state.py --authority ci

  lint-safe-syntax:
    name: Lint Safe Surface Syntax
    runs-on: ubuntu-latest
    timeout-minutes: 2

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Lint .safe test files
        working-directory: .
        run: |
          chmod +x scripts/lint_safe_syntax.sh
          scripts/lint_safe_syntax.sh

  frontend-smoke:
    name: Frontend Smoke
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Run frontend smoke checks
        working-directory: .
        run: python3 scripts/run_frontend_smoke.py

  pr05-d27-harness:
    name: PR05 D27 Harness
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR05 D27 harness
        working-directory: .
        run: python3 scripts/run_pr05_d27_harness.py

  pr06-ownership-harness:
    name: PR06 Ownership Harness
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR06 ownership harness
        working-directory: .
        run: python3 scripts/run_pr06_ownership_harness.py

  pr065-ada-mir-validator:
    name: PR06.5 Ada MIR Validator
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR06.5 Ada MIR validator gate
        working-directory: .
        run: python3 scripts/run_pr065_ada_mir_validator.py

  pr066-ada-mir-analyzer:
    name: PR06.6 Ada MIR Analyzer
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR06.6 Ada MIR analyzer gate
        working-directory: .
        run: python3 scripts/run_pr066_ada_mir_analyzer.py

  pr067-ada-check-no-python:
    name: PR06.7 Ada Check No Python
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      # The gate masks Python for `safec check` invocations and fails if the
      # check path attempts to spawn the Python backend.
      - name: Run PR06.7 Ada check cutover gate
        working-directory: .
        run: python3 scripts/run_pr067_ada_check_cutover.py

  pr068-ada-ast-emit-no-python:
    name: PR06.8 Ada AST Emit No Python
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      # Python remains allowed as repo glue. This job uses it only for the gate
      # script and AST validator around Ada-native `safec ast` / `safec emit`.
      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      # The gate masks `python` and `python3` on PATH for direct compiler
      # invocations and fails if `safec ast` or `safec emit` attempt to spawn
      # a Python backend.
      - name: Run PR06.8 Ada ast/emit cutover gate
        working-directory: .
        run: python3 scripts/run_pr068_ada_ast_emit_no_python.py

  pr0691-semantic-correctness:
    name: PR06.9.1 Semantic Correctness
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR06.9.1 semantic correctness gate
        working-directory: .
        run: python3 scripts/run_pr0691_semantic_correctness.py

  pr0692-lowering-cfg-integrity:
    name: PR06.9.2 Lowering CFG Integrity
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR06.9.2 lowering/CFG integrity gate
        working-directory: .
        run: python3 scripts/run_pr0692_lowering_cfg_integrity.py

  pr0693-runtime-boundary:
    name: PR06.9.3 Runtime Boundary
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      # Python remains allowed for the gate script and validators, but the
      # gate masks `python` and `python3` on PATH for every direct `safec`
      # runtime surface and fails on accidental backend spawns.
      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR06.9.3 runtime-boundary gate
        working-directory: .
        run: python3 scripts/run_pr0693_runtime_boundary.py

  pr0694-output-contract-stability:
    name: PR06.9.4 Output Contract Stability
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR06.9.4 output contract stability gate
        working-directory: .
        run: python3 scripts/run_pr0694_output_contract_stability.py

  pr0695-diagnostic-stability:
    name: PR06.9.5 Diagnostic Stability
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR06.9.5 diagnostic stability gate
        working-directory: .
        run: python3 scripts/run_pr0695_diagnostic_stability.py

  pr0696-unsupported-feature-boundary:
    name: PR06.9.6 Unsupported Feature Boundary
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR06.9.6 unsupported-feature boundary gate
        working-directory: .
        run: python3 scripts/run_pr0696_unsupported_feature_boundary.py

  pr0697-gate-quality:
    name: PR06.9.7 Regression Coverage and Gate Quality
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR06.9.7 regression coverage and gate quality
        working-directory: .
        run: python3 scripts/run_pr0697_gate_quality.py

  pr0698-legacy-package-cleanup:
    name: PR06.9.8 Dormant Legacy Package Cleanup
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR06.9.8 dormant legacy package cleanup gate
        working-directory: .
        run: python3 scripts/run_pr0698_legacy_package_cleanup.py

      - name: Verify committed PR06.9.8 evidence is up to date
        working-directory: .
        run: git diff --exit-code

  pr0699-build-reproducibility:
    name: PR06.9.9 Build and Reproducibility Hardening
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR06.9.9 build and reproducibility hardening
        working-directory: .
        run: python3 scripts/run_pr0699_build_reproducibility.py --authority ci

  pr06910-portability-environment:
    name: PR06.9.10 Portability and Environment Assumptions
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR06.9.10 portability and environment assumptions
        working-directory: .
        run: python3 scripts/run_pr06910_portability_environment.py

  pr06911-glue-script-safety:
    name: PR06.9.11 Glue Script Safety
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR06.9.11 glue script safety
        working-directory: .
        run: python3 scripts/run_pr06911_glue_script_safety.py

  pr06912-performance-scale-sanity:
    name: PR06.9.12 Performance and Scale Sanity
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR06.9.12 performance and scale sanity
        working-directory: .
        run: python3 scripts/run_pr06912_performance_scale_sanity.py

  pr06913-documentation-architecture-clarity:
    name: PR06.9.13 Documentation and Architectural Clarity
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR06.9.13 documentation and architecture clarity
        working-directory: .
        run: python3 scripts/run_pr06913_documentation_architecture_clarity.py

  pr07-rule5-result-safety:
    name: PR07 Rule 5 and Result Safety
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR07 Rule 5 and result safety gate
        working-directory: .
        run: python3 scripts/run_pr07_rule5_result_safety.py

      - name: Verify committed PR07 evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr07-rule5-result-safety-report.json

  pr081-local-concurrency-frontend:
    name: PR08.1 Local Concurrency Frontend
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR08.1 local concurrency frontend gate
        working-directory: .
        run: python3 scripts/run_pr081_local_concurrency_frontend.py

      - name: Verify committed PR08.1 evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr081-local-concurrency-frontend-report.json

  pr082-local-concurrency-analysis:
    name: PR08.2 Local Concurrency Analysis
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR08.2 local concurrency analysis gate
        working-directory: .
        run: python3 scripts/run_pr082_local_concurrency_analysis.py

      - name: Verify committed PR08.2 evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr082-local-concurrency-analysis-report.json

  pr083-interface-contracts:
    name: PR08.3 Interface Contracts
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR08.3 interface contracts gate
        working-directory: .
        run: python3 scripts/run_pr083_interface_contracts.py

      - name: Verify committed PR08.3 evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr083-interface-contracts-report.json

  pr083a-public-constants:
    name: PR08.3a Public Constants
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR08.3a public constants gate
        working-directory: .
        run: python3 scripts/run_pr083a_public_constants.py

      - name: Verify committed PR08.3a evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr083a-public-constants-report.json

  pr084-transitive-concurrency-integration:
    name: PR08.4 Transitive Concurrency Integration
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR08.4 transitive concurrency integration gate
        working-directory: .
        run: python3 scripts/run_pr084_transitive_concurrency_integration.py

      - name: Verify committed PR08.4 evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr084-transitive-concurrency-integration-report.json

  pr08-frontend-baseline:
    name: PR08 Frontend Baseline
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR08 frontend baseline gate
        working-directory: .
        run: python3 scripts/run_pr08_frontend_baseline.py

      - name: Verify committed PR08 baseline evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr08-frontend-baseline-report.json

  pr09a-emitter-surface:
    name: PR09a Emitter Surface
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR09a emitter surface gate
        working-directory: .
        run: python3 scripts/run_pr09a_emitter_surface.py

      - name: Verify committed PR09a emitter surface evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr09a-emitter-surface-report.json

  pr09a-emitter-mvp:
    name: PR09a Emitter MVP
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR09a emitter MVP gate
        working-directory: .
        run: python3 scripts/run_pr09a_emitter_mvp.py

      - name: Verify committed PR09a emitter MVP evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr09a-emitter-mvp-report.json

  pr09b-sequential-semantics:
    name: PR09b Sequential Semantics
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR09b sequential semantics gate
        working-directory: .
        run: python3 scripts/run_pr09b_sequential_semantics.py

      - name: Verify committed PR09b sequential evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr09b-sequential-semantics-report.json

  pr09b-concurrency-output:
    name: PR09b Concurrency Output
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR09b concurrency output gate
        working-directory: .
        run: python3 scripts/run_pr09b_concurrency_output.py

      - name: Verify committed PR09b concurrency evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr09b-concurrency-output-report.json

  pr09b-snapshot-refresh:
    name: PR09b Snapshot Refresh
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR09b snapshot refresh gate
        working-directory: .
        run: python3 scripts/run_pr09b_snapshot_refresh.py

      - name: Verify committed PR09b snapshot evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr09b-snapshot-refresh-report.json

  pr09-ada-emission-baseline:
    name: PR09 Ada Emission Baseline
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR09 Ada emission baseline gate
        working-directory: .
        run: python3 scripts/run_pr09_ada_emission_baseline.py

      - name: Verify committed PR09 baseline evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr09-ada-emission-baseline-report.json

  pr10-contract-baseline:
    name: PR10 Contract Baseline
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Run PR10 contract baseline gate
        working-directory: .
        run: python3 scripts/run_pr10_contract_baseline.py

      - name: Verify committed PR10 contract evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr10-contract-baseline-report.json

  pr10-emitted-flow:
    name: PR10 Emitted Flow
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Install GNATprove
        working-directory: .
        run: |
          alr install gnatprove=14.1.1
          echo "${HOME}/.alire/bin" >> "$GITHUB_PATH"

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR10 emitted flow gate
        working-directory: .
        run: python3 scripts/run_pr10_emitted_flow.py

      - name: Verify committed PR10 emitted flow evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr10-emitted-flow-report.json

  pr10-emitted-prove:
    name: PR10 Emitted Prove
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Install GNATprove
        working-directory: .
        run: |
          alr install gnatprove=14.1.1
          echo "${HOME}/.alire/bin" >> "$GITHUB_PATH"

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR10 emitted prove gate
        working-directory: .
        run: python3 scripts/run_pr10_emitted_prove.py

      - name: Verify committed PR10 emitted prove evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr10-emitted-prove-report.json

  pr10-emitted-baseline:
    name: PR10 Emitted Baseline
    runs-on: ubuntu-latest
    timeout-minutes: 150

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Install GNATprove
        working-directory: .
        run: |
          alr install gnatprove=14.1.1
          echo "${HOME}/.alire/bin" >> "$GITHUB_PATH"

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR10 emitted baseline gate
        working-directory: .
        run: python3 scripts/run_pr10_emitted_baseline.py

      - name: Verify committed PR10 baseline evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr10-emitted-baseline-report.json

  emitted-hardening-regressions:
    name: Emitted Hardening Regressions
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Install GNATprove
        working-directory: .
        run: |
          alr install gnatprove=14.1.1
          echo "${HOME}/.alire/bin" >> "$GITHUB_PATH"

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run supplemental emitted hardening regressions
        working-directory: .
        run: python3 scripts/run_emitted_hardening_regressions.py

      - name: Verify committed hardening evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/emitted-hardening-regressions-report.json

  pr101-comprehensive-audit:
    name: PR10.1 Comprehensive Audit
    runs-on: ubuntu-latest
    timeout-minutes: 210

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Install GNATprove
        working-directory: .
        run: |
          alr install gnatprove=14.1.1
          echo "${HOME}/.alire/bin" >> "$GITHUB_PATH"

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR10.1 comprehensive audit gate
        working-directory: .
        run: python3 scripts/run_pr101_comprehensive_audit.py

      - name: Verify committed PR10.1 audit evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr101-comprehensive-audit-report.json

  pr102-rule5-boundary-closure:
    name: PR10.2 Rule 5 Boundary Closure
    runs-on: ubuntu-latest
    timeout-minutes: 210

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Install GNATprove
        working-directory: .
        run: |
          alr install gnatprove=14.1.1
          echo "${HOME}/.alire/bin" >> "$GITHUB_PATH"

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR10.2 Rule 5 boundary-closure gate
        working-directory: .
        run: python3 scripts/run_pr102_rule5_boundary_closure.py

      - name: Verify committed PR10.2 evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr102-rule5-boundary-closure-report.json

  pr103-sequential-proof-expansion:
    name: PR10.3 Sequential Proof Expansion
    runs-on: ubuntu-latest
    timeout-minutes: 240

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Install GNATprove
        working-directory: .
        run: |
          alr install gnatprove=14.1.1
          echo "${HOME}/.alire/bin" >> "$GITHUB_PATH"

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR10.3 ownership proof-expansion gate
        working-directory: .
        run: python3 scripts/run_pr103_sequential_proof_expansion.py

      - name: Verify committed PR10.3 evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr103-sequential-proof-expansion-report.json

  pr104-gnatprove-evidence-parser-hardening:
    name: PR10.4 GNATprove Evidence and Parser Hardening
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Install GNATprove
        working-directory: .
        run: |
          alr install gnatprove=14.1.1
          echo "${HOME}/.alire/bin" >> "$GITHUB_PATH"

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR10.4 evidence/parser-hardening gate
        working-directory: .
        run: python3 scripts/run_pr104_gnatprove_evidence_parser_hardening.py

      - name: Verify committed PR10.4 evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr104-gnatprove-evidence-parser-hardening-report.json

  pr105-ada-emitter-maintenance-hardening:
    name: PR10.5 Ada Emitter Maintenance Hardening
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Install GNATprove
        working-directory: .
        run: |
          alr install gnatprove=14.1.1
          echo "${HOME}/.alire/bin" >> "$GITHUB_PATH"

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR10.5 Ada emitter maintenance-hardening gate
        working-directory: .
        run: python3 scripts/run_pr105_ada_emitter_maintenance_hardening.py

      - name: Verify committed PR10.5 evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr105-ada-emitter-maintenance-hardening-report.json

  pr106-sequential-proof-corpus-expansion:
    name: PR10.6 Sequential Proof-Corpus Expansion
    runs-on: ubuntu-latest
    timeout-minutes: 240

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Install GNATprove
        working-directory: .
        run: |
          alr install gnatprove=14.1.1
          echo "${HOME}/.alire/bin" >> "$GITHUB_PATH"

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR10.6 sequential proof-corpus expansion gate
        working-directory: .
        run: python3 scripts/run_pr106_sequential_proof_corpus_expansion.py

      - name: Verify committed PR10.6 evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr106-sequential-proof-corpus-expansion-report.json

  pr111-language-evaluation-harness:
    name: PR11.1 Language Evaluation Harness
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR11.1 language-evaluation harness gate
        working-directory: .
        run: python3 scripts/run_pr111_language_evaluation_harness.py

      - name: Verify committed PR11.1 evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr111-language-evaluation-harness-report.json

  pr112-parser-completeness-phase1:
    name: PR11.2 Parser Completeness Phase 1
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR11.2 parser completeness phase 1 gate
        working-directory: .
        run: python3 scripts/run_pr112_parser_completeness_phase1.py

      - name: Verify committed PR11.2 evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr112-parser-completeness-phase1-report.json

  pr113-discriminated-types-tuples-structured-returns:
    name: PR11.3 Discriminated Types, Tuples, and Structured Returns
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR11.3 discriminated types, tuples, and structured returns gate
        working-directory: .
        run: python3 scripts/run_pr113_discriminated_types_tuples_structured_returns.py

      - name: Verify committed PR11.3 evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr113-discriminated-types-tuples-structured-returns-report.json

  pr113a-proof-checkpoint1:
    name: PR11.3a Proof Checkpoint 1
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Install GNATprove
        working-directory: .
        run: |
          alr install gnatprove=14.1.1
          echo "${HOME}/.alire/bin" >> "$GITHUB_PATH"

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR11.3a proof checkpoint 1 gate
        working-directory: .
        run: python3 scripts/run_pr113a_proof_checkpoint1.py

      - name: Verify committed PR11.3a evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr113a-proof-checkpoint1-report.json

  pr114-signature-control-flow-syntax:
    name: PR11.4 Signature and Control-Flow Syntax
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          version: 2.1.0
          toolchain: gnat_native gprbuild

      - name: Build compiler frontend
        working-directory: .
        run: cd compiler_impl && alr build

      - name: Run PR11.4 signature/control-flow syntax gate
        working-directory: .
        run: python3 scripts/run_pr114_signature_control_flow_syntax.py

      - name: Verify committed PR11.4 evidence is up to date
        working-directory: .
        run: git diff --exit-code execution/reports/pr114-signature-control-flow-syntax-report.json

  spark-verify:
    name: Build & Verify SPARK Companion
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      # ------------------------------------------------------------------
      # Checkout
      # ------------------------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      # ------------------------------------------------------------------
      # Install GNAT and GNATprove via Alire
      # ------------------------------------------------------------------
      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          toolchain: gnat_native gprbuild

      - name: Initialize Alire workspace and install GNATprove
        run: |
          alr install gnatprove=14.1.1
          echo "${HOME}/.alire/bin" >> "$GITHUB_PATH"

      - name: Verify tools
        run: |
          alr exec -- gprbuild --version | head -1
          gnatprove --version | head -1

      - name: Record toolchain version for cache key
        id: toolchain-version
        run: echo "gnatprove=$(gnatprove --version 2>&1 | head -1 || echo unknown)" >> "$GITHUB_OUTPUT"

      # ------------------------------------------------------------------
      # Verify frozen commit SHA
      # ------------------------------------------------------------------
      - name: Verify frozen commit SHA
        working-directory: .
        run: |
          REPO_SHA="$(cat meta/commit.txt | tr -d '[:space:]')"
          echo "Frozen SHA from meta/commit.txt: ${REPO_SHA}"
          echo "Expected SHA from workflow env:  ${FROZEN_SHA}"
          if [[ "${REPO_SHA}" != "${FROZEN_SHA}" ]]; then
            echo "ERROR: Frozen SHA mismatch. meta/commit.txt=${REPO_SHA} != env.FROZEN_SHA=${FROZEN_SHA}"
            exit 1
          fi

      # ------------------------------------------------------------------
      # Cache GNATprove results
      # ------------------------------------------------------------------
      - name: Cache GNATprove results
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          path: |
            companion/gen/obj/gnatprove
          key: gnatprove-${{ runner.os }}-${{ steps.toolchain-version.outputs.gnatprove }}-${{ hashFiles('companion/spark/*.ads', 'companion/spark/*.adb', 'companion/gen/companion.gpr') }}
          restore-keys: |
            gnatprove-${{ runner.os }}-

      # ------------------------------------------------------------------
      # Ensure build directory exists
      # ------------------------------------------------------------------
      - name: Prepare build directory
        run: mkdir -p obj

      # ------------------------------------------------------------------
      # Make scripts executable
      # ------------------------------------------------------------------
      - name: Make scripts executable
        working-directory: .
        run: chmod +x scripts/*.sh

      # ------------------------------------------------------------------
      # Step 1: Compile
      # ------------------------------------------------------------------
      - name: "Step 1: Compile (alr build)"
        run: alr build

      # ------------------------------------------------------------------
      # Step 2: GNATprove flow analysis (Bronze gate)
      # ------------------------------------------------------------------
      - name: "Step 2: GNATprove flow analysis"
        run: alr exec -- gnatprove -P companion.gpr --mode=flow --report=all --warnings=error

      # ------------------------------------------------------------------
      # Step 3: GNATprove proof analysis (Silver gate)
      # ------------------------------------------------------------------
      - name: "Step 3: GNATprove proof"
        run: alr exec -- gnatprove -P companion.gpr --mode=prove --level=2 --prover=cvc5,z3,altergo --steps=0 --timeout=120 --report=all --warnings=error --checks-as-errors=on

      # ------------------------------------------------------------------
      # Step 4: Extract assumptions
      # ------------------------------------------------------------------
      - name: "Step 4: Extract assumptions"
        working-directory: .
        run: scripts/extract_assumptions.sh

      # ------------------------------------------------------------------
      # Step 5: Diff assumptions against baseline
      # ------------------------------------------------------------------
      - name: "Step 5: Diff assumptions"
        working-directory: .
        run: scripts/diff_assumptions.sh

      # ------------------------------------------------------------------
      # Upload artifacts on failure for debugging
      # ------------------------------------------------------------------
      - name: Upload GNATprove output on failure
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: gnatprove-output
          path: |
            companion/gen/obj/gnatprove/
            companion/assumptions_extracted.txt
          retention-days: 14

      # ------------------------------------------------------------------
      # Upload assumptions extraction as artifact
      # ------------------------------------------------------------------
      - name: Upload assumptions extraction
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: assumptions-report
          path: |
            companion/assumptions_extracted.txt
            companion/assumptions.yaml
            companion/gen/prove_golden.txt
            companion/gen/obj/gnatprove/gnatprove.out
          retention-days: 30

  # ====================================================================
  # Verified Emission Templates
  # ====================================================================
  templates-verify:
    name: Build & Verify Emission Templates
    runs-on: ubuntu-latest
    timeout-minutes: 30

    defaults:
      run:
        working-directory: companion/templates

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Install Alire
        uses: alire-project/setup-alire@b607671067a8ca5d7b7f4a12dadb4dc009cd2cc7 # v3
        with:
          toolchain: gnat_native gprbuild

      - name: Initialize Alire workspace and install GNATprove
        run: |
          alr install gnatprove=14.1.1
          echo "${HOME}/.alire/bin" >> "$GITHUB_PATH"

      - name: Record toolchain version for cache key
        id: toolchain-version
        run: echo "gnatprove=$(gnatprove --version 2>&1 | head -1 || echo unknown)" >> "$GITHUB_OUTPUT"

      - name: Cache GNATprove results
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          path: |
            companion/templates/obj/gnatprove
          key: gnatprove-templates-${{ runner.os }}-${{ steps.toolchain-version.outputs.gnatprove }}-${{ hashFiles('companion/spark/*.ads', 'companion/spark/*.adb', 'companion/templates/*.ads', 'companion/templates/*.adb', 'companion/templates/templates.gpr') }}
          restore-keys: |
            gnatprove-templates-${{ runner.os }}-

      - name: Prepare build directory
        run: mkdir -p obj

      - name: Make scripts executable
        working-directory: .
        run: chmod +x scripts/*.sh

      - name: "Step 1: Compile (alr build)"
        run: alr build

      - name: "Step 2: GNATprove flow analysis"
        run: alr exec -- gnatprove -P templates.gpr --mode=flow --report=all --warnings=error

      - name: "Step 3: GNATprove proof"
        run: alr exec -- gnatprove -P templates.gpr --mode=prove --level=2 --prover=cvc5,z3,altergo --steps=0 --timeout=120 --report=all --warnings=error --checks-as-errors=on

      # ----------------------------------------------------------------
      # Step 4-5: Assumption extraction and diff (matches spark-verify)
      # ----------------------------------------------------------------
      - name: "Step 4: Extract assumptions"
        working-directory: .
        env:
          PROVE_OUT: companion/templates/obj/gnatprove
        run: scripts/extract_assumptions.sh

      - name: "Step 5: Diff assumptions"
        working-directory: .
        env:
          PROVE_GOLDEN: companion/templates/prove_golden.txt
          PROVE_OUT: companion/templates/obj/gnatprove/gnatprove.out
        run: scripts/diff_assumptions.sh

      # ----------------------------------------------------------------
      # Artifacts
      # ----------------------------------------------------------------
      - name: Upload GNATprove output on failure
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: templates-gnatprove-output
          path: |
            companion/templates/obj/gnatprove/
            companion/assumptions_extracted.txt
          retention-days: 14

      - name: Upload templates proof report
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: templates-proof-report
          path: |
            companion/assumptions_extracted.txt
            companion/assumptions.yaml
            companion/templates/prove_golden.txt
            companion/templates/obj/gnatprove/gnatprove.out
          retention-days: 30
