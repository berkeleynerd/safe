package factorial

   subtype count is integer (0 to 7);
   subtype value is integer (1 to 5_040);
   type value_table is array (count) of value;

   values : value_table = (1, 1, 2, 6, 24, 120, 720, 5_040);

   function compute (n : count) returns value
      return values (n)
