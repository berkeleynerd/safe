package opcode_dispatch

   type opcode is binary (8);
   code : opcode = opcode (1);

   function render (op : opcode) returns string

      case op
         when opcode (0)
            return "nop"

         when opcode (1)
            return "load"

         when others
            return "unknown"

   print (render (code))
