package First_Diagnostic_Order is

   type Value is range -100 to 100;

   function Pick (A, B, C : Value) returns Integer is
   begin
      if A > 0 then
         return Integer (A) / B;
      else
         return Integer (A) / C;
      end if;
   end Pick;

end First_Diagnostic_Order;
