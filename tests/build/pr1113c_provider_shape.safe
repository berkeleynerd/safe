package pr1113c_provider_shape

   public type shape is circle (radius : integer) or idle;

   function score (value : shape) returns integer

      match value
         when circle (radius)
            return radius
         when idle
            return 0

   print (score (circle (3)));
   print (score (idle));
