with provider_sum_shape

package client_sum_shape

   current : provider_sum_shape.shape = provider_sum_shape.circle (4)

   function score (value : provider_sum_shape.shape) returns integer

      match value
         when circle (radius)
            return radius
         when idle
            return 0

   result : integer = score (current)
