package provider_enum

   public type color is (red, green, blue);
   public default_color : constant color = red;

   public function next (value : color) returns color

      case value
         when red
            return green;
         when green
            return blue;
         when others
            return red;
