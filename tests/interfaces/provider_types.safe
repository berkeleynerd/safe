package provider_types

   public type count is range 0 to 100;
   public type handle;
   public subtype small_count is count;

   public function next (value : count) returns count

      return value;


